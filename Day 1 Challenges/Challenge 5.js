/*
Why does this code not work ? Edit until it works.
*/

const myArray = [20,30,40,50];
console.log(myArray[3]);

// the code is invalid because it is accessing the element which is not present
//either add one  more element in the array
//change the index number that access only the elements present in the array